{
  "name": "creative-coding-lab",
  "version": "1.0.0",
  "description": "Creative coding learning app for high school students using Blockly + p5.js",
  "private": true,
  "scripts": {
    "dev":   "npx serve . -p 3000",
    "build": "echo 'Static site — no build step needed'",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "serve": "^14.2.1",
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
